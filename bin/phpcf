#!/usr/bin/env sh

readonly PROJECT_DIRECTORY=$(realpath $(dirname $(realpath $0))/..)

set -e

docker run \
    --rm \
    -t \
    -v ${PROJECT_DIRECTORY}:/var/php-code-fixer:ro \
    -w /var/php-code-fixer \
    steevanb/php-code-fixer:2.0.19 \
    phpcf --exclude=/vendor/ --target=7.3 /var/php-code-fixer
